{
  "name": "mobile-nav",
  "title": "MobileNav",
  "family": "navigation",
  "description": "A navigation drawer behind a trigger, for viewports too narrow for a rail.",
  "status": {
    "css": {
      "state": "latest"
    },
    "react": {
      "state": "latest"
    },
    "figma": {
      "state": "future"
    }
  },
  "dependencies": [
    "state-layer",
    "icon",
    "side-nav"
  ],
  "files": [
    {
      "source": "packages/css/src/mobile-nav.css",
      "target": "styles/mobile-nav.css"
    },
    {
      "source": "packages/react/src/mobile-nav.tsx",
      "target": "components/mobile-nav.tsx"
    },
    {
      "source": "packages/react/src/cx.ts",
      "target": "lib/cx.ts"
    },
    {
      "source": "packages/react/src/scroll-lock.ts",
      "target": "lib/scroll-lock.ts"
    }
  ],
  "tier": "free",
  "behavior": {
    "summary": "A trigger and a native `<dialog>` opened with `showModal()`, holding a `SideNav`. The browser supplies the focus trap, `inert` on the page behind, the backdrop, Escape, and focus returned to the trigger — the same reasoning Dialog follows, and the same reason not to hand-roll any of it.",
    "decisions": [
      {
        "decision": "It is modal, and a real <dialog>",
        "why": "A drawer covering the page is modal whether or not it is called one: the content behind it must not be reachable by Tab, by a screen reader's own navigation, or by scrolling. `showModal()` supplies all three, plus the focus trap that is the most bug-prone thing anyone writes by hand. A non-modal panel would leave a reader tabbing into a page they cannot see."
      },
      {
        "decision": "It renders a SideNav rather than reimplementing one",
        "why": "The drawer's content is a rail — sections, nested groups, current-page marking, the lot. Rebuilding that markup here would be a second copy to keep in step, and the two would drift on exactly the details that matter: which element carries the group name, and how the current page is announced."
      },
      {
        "decision": "Picking a destination closes it",
        "why": "A drawer that stays open over the page you just navigated to is the commonest complaint about this pattern, and under client-side routing nothing else closes it — the document never unloads. Handled here rather than left to every caller, because every caller wants it."
      },
      {
        "decision": "It does not decide when it applies",
        "why": "There are no breakpoint tokens in this system, deliberately: where a rail stops fitting depends on the rail's width and the page around it, which is the page's knowledge. So this hides itself at no width — the caller's own media query does that, the same way SideNav takes its width from the page."
      },
      {
        "decision": "The scroll lock is shared with Dialog, not its own",
        "why": "Two components each counting their own holders would fight: with a Dialog and a drawer both open, whichever closed first would unlock the page while the other still covered it. One counter in one module, which is why that helper moved out of Dialog when this was built."
      }
    ]
  },
  "props": {
    "sections": {
      "summary": "The destinations, grouped — the same shape SideNav takes.",
      "use": [
        "The same array you give the rail, so the two cannot disagree about what the product contains."
      ],
      "dont": [
        "A different set from the rail's. Two navigations that list different things is worse than either alone."
      ],
      "a11y": "Rendered by `SideNav` inside the drawer, so the group naming, the nested disclosures and the current-page marking are the rail's, not a second implementation of them."
    },
    "label": {
      "summary": "Accessible name for the navigation landmark inside the drawer.",
      "use": [
        "Naming it for what it contains, as the other navs are named."
      ],
      "dont": [
        "Leaving the default when the page's TopNav is also called Main."
      ],
      "a11y": "Passed to the `SideNav`. The dialog itself is named by `title`."
    },
    "title": {
      "summary": "The drawer's own name, shown at its top.",
      "use": [
        "A product name, or what this navigation covers."
      ],
      "dont": [
        "Leaving it out. A modal with no accessible name is announced as \"dialog\" and nothing else."
      ],
      "a11y": "Wired with `aria-labelledby` on the dialog, and rendered as a paragraph rather than a heading — the drawer sits in the page's outline and cannot know what level it is at, the same reasoning Notice's title follows."
    },
    "triggerLabel": {
      "summary": "Accessible name for the control that opens the drawer.",
      "use": [
        "Translating it, or naming what is being opened when a page has more than one drawer."
      ],
      "dont": [
        "An empty string. The trigger is icon-only, so this is the only name it has."
      ],
      "a11y": "Becomes the trigger's `aria-label`, alongside `aria-expanded` and `aria-controls`."
    },
    "open": {
      "summary": "Whether the drawer is showing. Makes the component controlled.",
      "use": [
        "Closing it from your router when a navigation completes, if you do not want the built-in behaviour."
      ],
      "dont": [
        "Passing it alongside `defaultOpen`."
      ],
      "conflicts": [
        "defaultOpen"
      ]
    },
    "defaultOpen": {
      "summary": "Starting state for an uncontrolled drawer.",
      "use": [
        "Almost never. A drawer open on arrival covers the page someone came to read."
      ],
      "conflicts": [
        "open"
      ]
    },
    "onOpenChange": {
      "summary": "Called when the drawer should open or close.",
      "use": [
        "Controlled mode, to set `open`.",
        "Reacting — closing a search, or logging."
      ],
      "dont": [
        "Using it to close the drawer on navigation. It already does that."
      ]
    },
    "closeLabel": {
      "summary": "Accessible name for the drawer's close control.",
      "use": [
        "Translating it."
      ],
      "dont": [
        "An empty string — the control is icon-only."
      ]
    },
    "footer": {
      "summary": "Content pinned below the destinations — an account link, a sign-out.",
      "use": [
        "The things a top nav would put in its actions slot, which has nowhere else to go at this width."
      ],
      "dont": [
        "Destinations. Those belong in `sections`, inside the nav landmark where they can be found."
      ],
      "a11y": "Outside the `SideNav`'s landmark, because none of it is navigation."
    },
    "className": {
      "summary": "Extra classes on the trigger's wrapper, for placement — not for restyling it.",
      "use": [
        "The media query that decides at what width this appears at all."
      ],
      "dont": [
        "Styling the drawer through it. The drawer is in the top layer and is not a descendant of this."
      ]
    }
  },
  "usage": [
    {
      "case": "A drawer beside a rail, sharing one set of destinations",
      "when": "The commonest case: the same navigation, presented two ways.",
      "example": "const sections = [\n  {\n    items: [\n      { label: \"Invoices\", href: \"/invoices\", current: true },\n      { label: \"Clients\", href: \"/clients\" },\n    ],\n  },\n];\n\n{/* The media queries are the page's — this system has no breakpoint\n    tokens, because where a rail stops fitting depends on the rail. */}\n<MobileNav className=\"narrow-only\" title=\"Ratā\" label=\"Main\" sections={sections} />\n<SideNav className=\"wide-only\" label=\"Main\" sections={sections} />",
      "notes": "One array, two presentations. Two navigations listing different things is worse than either alone, which is why the same value goes to both rather than each being given its own."
    },
    {
      "case": "A drawer with account actions under the destinations",
      "when": "At this width there is no top bar to put them in.",
      "example": "<MobileNav\n  title=\"Ratā\"\n  sections={sections}\n  footer={\n    <>\n      <Button variant=\"secondary\">Settings</Button>\n      <Button variant=\"tertiary\">Sign out</Button>\n    </>\n  }\n/>",
      "notes": "`footer` sits outside the nav landmark, because none of it is navigation — the same split TopNav makes between `items` and `actions`."
    },
    {
      "case": "Don't: a non-modal drawer over a live page",
      "when": "Never — this is the shape to recognise and avoid.",
      "example": "{/* Don't: this is what NOT using MobileNav looks like. */}\n<div className={open ? \"drawer open\" : \"drawer\"}>\n  <SideNav label=\"Main\" sections={sections} />\n</div>",
      "notes": "Don't. A drawer covering the page is modal whether or not it is called one: without `showModal()` the content behind stays in the tab order, a screen reader walks straight into a page nobody can see, and the page scrolls under the drawer. Those three are what this component exists to get from the platform rather than approximate."
    }
  ],
  "tokens": {
    "trigger": {
      "summary": "The control that opens the drawer. Same construction as the other icon-only controls here: transparent, taking its colour from the text.",
      "tokens": {
        "glyph size": "size.icon.md",
        "target inline-size / block-size": "size.control.md",
        "border-radius": "radius.element",
        "colour": "theme.fg.primary",
        "hover / press": "compose the .rata-state-layer class",
        "focus ring": "theme.focus-ring at focus.ring-width, offset focus.ring-offset, on :focus-visible"
      }
    },
    "drawer": {
      "summary": "The panel itself: full height, against the inline-start edge, at a width that leaves the page visible behind the scrim so it still reads as a layer over it.",
      "tokens": {
        "background": "theme.bg.surface",
        "box-shadow": "theme.elevation.modal",
        "max-inline-size": "size.control.lg multiplied by 8, or the viewport less one large control",
        "padding-block": "space.padding.md",
        "padding-inline": "space.padding.sm",
        "gap between header, nav and footer": "space.stack.md",
        "enter transition": "motion.modal.duration with motion.easing.enter",
        "exit transition": "motion.modal.duration with motion.easing.exit"
      }
    },
    "backdrop": {
      "summary": "The wash over the inert page behind it — the same token and the same reasoning as Dialog's.",
      "tokens": {
        "background": "theme.scrim",
        "fade": "motion.modal.duration with motion.easing.enter"
      }
    },
    "header": {
      "summary": "The drawer's name and its close control.",
      "tokens": {
        "title font-size": "type.heading.size",
        "title font-weight": "type.heading.weight",
        "gap": "space.gap.sm",
        "close target": "size.control.sm"
      }
    }
  }
}

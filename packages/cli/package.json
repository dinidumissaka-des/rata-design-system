{
  "name": "@rata/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Copy-paste distribution CLI: lists registry components, copies their source into a consumer project, and answers agent lookups (props/tokens/pages) straight from source.",
  "bin": {
    "rata": "./bin/rata.mjs"
  },
  "files": [
    "bin",
    "lib"
  ],
  "scripts": {
    "build": "node -e \"process.exit(0)\""
  }
}

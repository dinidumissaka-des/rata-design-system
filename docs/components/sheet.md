<!-- GENERATED by scripts/generate-component-docs.mjs — do not edit by hand. -->
<!-- Written half lives in registry/components/<name>.json; derived half is parsed from packages/react/src/<name>.tsx. -->

# Sheet

A modal surface anchored to an edge of the viewport — a bottom sheet or a side drawer.

```tsx
import { Sheet } from "@rata/react";
```

| | |
|---|---|
| Registry name | `sheet` |
| Family | overlays |
| Tier | free |
| Status (css / react / figma) | latest / latest / future |
| Depends on | `state-layer`, `icon`, `button` |

## Behavior

A native `<dialog>` opened with `showModal()`, held against one edge of the viewport. The browser supplies the focus trap, `inert` on the page behind, the top layer, `::backdrop`, Escape, and focus returned to whatever opened it — the same reasoning Dialog and MobileNav follow, and the same reason not to hand-roll any of it.

- **It is modal, and there is no option for it not to be** — MobileNav's contract already states the rule this follows: a surface covering the page is modal whether or not it is called one, because the content behind it must not be reachable by Tab, by a screen reader's own navigation, or by scrolling. A non-modal edge-anchored overlay would leave a reader tabbing into a page they cannot see. A panel that genuinely should not be modal belongs in the layout, which is what Panel is for.
- **`edge` rather than separate BottomSheet and Drawer components** — Everything that makes this hard — the top layer, the focus trap, the scroll lock with the scrollbar held open, the exit that must keep the layer alive — is identical on all four edges. What differs is which axis it slides along and which two corners are rounded, and that is geometry, not behaviour. Splitting by edge would be three copies of the difficult part to keep in step, which is the mistake Tabs and SegmentedControl were separated to avoid making in the other direction: they were split because their SEMANTICS differ, not their looks.
- **`size` is one measure on whichever axis the sheet slides along** — A bottom sheet's size is its height; a drawer's is its width. They are the same question — how far does it come in — asked about different axes, so one prop answers it and the component knows which axis from `edge`. Two props would let a caller state the one that does not apply, which the type system cannot catch and which would silently do nothing.
- **It reuses Dialog's scroll lock rather than its own** — That lock counts holders instead of saving and restoring, because save-and-restore breaks the moment two things overlap — the second captures `hidden` as the value to put back, and closing it leaves the page locked with nothing open. A sheet over a dialog is an ordinary combination, so a second independent lock would reintroduce exactly that bug. It also holds the scrollbar's width open, measured at lock time because it is whatever the browser and the reader's settings make it.
- **No drag to dismiss in this version** — Close button, Escape and backdrop click are all supplied by the platform and all operable by keyboard and switch access. A drag gesture is a pointer state machine with a velocity threshold, a scroll-versus-drag conflict inside the sheet's own body, and no keyboard equivalent — so it needs its own pass through these gates rather than riding along with the geometry. No grab handle is drawn either: an affordance that does nothing is worse than none.
- **Escape is prevented, then reported** — Dialog's reasoning, unchanged: the platform would close the dialog itself and leave `open` still saying it is showing. Preventing the default and reporting through `onClose` keeps React the single source of truth.
- **A native close is reported rather than ignored** — `<form method="dialog">` is the documented HTML way to close a dialog and the platform can close one for its own reasons too. Neither goes through `onClose`, and the driving effect is keyed on `open`, so it never re-runs to notice — which in Dialog left the scroll lock on the body with nothing open and the page unable to scroll at all. The native `close` event reports `external`, and only while React still believes the sheet is open.
- **Initial focus is a ref, because React's `autoFocus` cannot work here** — React implements `autoFocus` by calling `.focus()` during commit rather than emitting the HTML attribute, so `showModal()` — which runs afterwards, in an effect — never sees an `[autofocus]` element and applies its own default, undoing it. `initialFocus` is applied after `showModal()` and therefore wins.
- **The title is a real `<h2>`, as Dialog's is** — Everything behind a modal is inert, so the outline inside it genuinely starts fresh and the title is the top of it. This is the difference from Panel, which sits inside the page's outline and has to be told its level.

## Props

Extends `Omit<`.

| Prop | Type | Default | Summary |
|---|---|---|---|
| `title` | `ReactNode` | — | The sheet's name. Required. |
| `children` | `ReactNode` | — | The sheet's content. |
| `open` | `boolean` | — | Whether the sheet is showing. Always controlled. |
| `onClose` | `(reason: SheetCloseReason) => void` | — | Called when the sheet asks to close, with why. |
| `edge?` | `SheetEdge` | `"block-end"` | Which edge the sheet is anchored to and slides in from. |
| `size?` | `SheetSize` | `"md"` | How far the sheet comes in, measured on the axis it slides along. |
| `description?` | `ReactNode` | — | A line under the title, wired as the sheet's description. |
| `footer?` | `ReactNode` | — | The actions. |
| `dismissible?` | `boolean` | `true` | Whether the reader can close it themselves. |
| `dismissLabel?` | `string` | `"Close"` | Accessible name for the close button. |
| `initialFocus?` | `RefObject<HTMLElement \| null>` | — | What to focus when it opens. Defaults to the platform's choice. |
| `className?` | `string` | — | Extra classes on the sheet, for placement — not for restyling it. |

### `title`

```ts
title: ReactNode
```

The sheet's name. Required.

**Use when**

- A noun phrase naming what this is about: "Filters", "Token documentation".

**Don't use for**

- Omitting it and relying on the content. A modal with no accessible name announces as "dialog" and nothing else.

**Accessibility** Rendered as an `<h2>` and wired with `aria-labelledby`, so the sheet is named the moment it opens.

### `children`

```ts
children: ReactNode
```

The sheet's content.

**Use when**

- The detail, the form, the list of options.

**Don't use for**

- Another sheet's trigger, if you can avoid it. Stacked modals are legal and the scroll lock counts holders for that reason, but a reader two layers deep has lost the page.

**Accessibility** Scrolls in its own region so the title stays put and the close control stays reachable.

### `open`

```ts
open: boolean
```

Whether the sheet is showing. Always controlled.

Source doc: Whether the sheet is showing. Always controlled — `showModal()` has no prop.

**Use when**

- State the caller owns, so opening and closing are things that happen in their code rather than inside this one.

**Don't use for**

- Leaving it stale after `onClose`. The sheet reports; the caller decides.

**Accessibility** Drives `showModal()` and `close()`. Nothing else makes the sheet modal — rendering the `open` attribute instead shows it non-modally, with no focus trap and a live page behind it.

### `onClose`

```ts
onClose: (reason: SheetCloseReason) => void
```

Called when the sheet asks to close, with why.

Source doc: Called when the sheet asks to close. Set `open` to false in it.

**Use when**

- Setting `open` to false.

**Don't use for**

- Ignoring the reason when a backdrop click should be treated differently from the close button — an unsaved form is the usual case.

**Accessibility** Focus returns to whatever opened the sheet automatically, because the platform does it.

### `edge`

```ts
edge?: SheetEdge = "block-end"
```

Which edge the sheet is anchored to and slides in from.

Source doc: Which edge it is held against. `block-end` is the bottom sheet.

**Use when**

- `block-end` for a bottom sheet — options and detail on a narrow viewport, where the bottom is where a thumb is.
- `inline-end` for a detail drawer on a wide viewport, where it is the same side a Panel would sit on — so the same content can move between the two as the viewport changes.

**Don't use for**

- `block-start` for anything a reader dismisses often. The top edge is where the browser's own chrome is, and the reach is worst there.

**Accessibility** Logical edges, not physical: `inline-start` is the left in a left-to-right document and the right in a right-to-left one, so a drawer follows the reading direction without the caller doing anything.

### `size`

```ts
size?: SheetSize = "md"
```

How far the sheet comes in, measured on the axis it slides along.

Source doc: How far it comes in, measured on the axis it slides along.

**Use when**

- `sm` for a short list of actions; `lg` for a form or a document.

**Don't use for**

- Expecting it to control the other axis. A sheet spans the edge it is anchored to, always.

**Accessibility** Every size gives way to a small viewport rather than overflowing it, and never covers the edge it slid from.

### `description`

```ts
description?: ReactNode
```

A line under the title, wired as the sheet's description.

**Use when**

- One sentence of consequence or scope that the title cannot carry.

**Don't use for**

- Content. This is announced with the name, so length here is length before the reader reaches anything.

**Accessibility** Becomes `aria-describedby`. Omitted, there is no dangling reference.

### `footer`

```ts
footer?: ReactNode
```

The actions.

Source doc: The actions. Reads after the body they act on.

**Use when**

- An Apply and a Cancel for a filter sheet.

**Don't use for**

- Navigation. A sheet is a place to do one thing and leave.

**Accessibility** Rendered after the body in DOM order, so the actions are reached after the content they act on.

### `dismissible`

```ts
dismissible?: boolean = true
```

Whether the reader can close it themselves.

**Use when**

- `false` only while something must finish — a migration step, a required choice with no default.

**Don't use for**

- `false` to keep attention. A sheet the reader cannot leave is a trap, and they will reload the page instead.

**Accessibility** `false` removes the close button and prevents the `cancel` event, so Escape and a backdrop click do nothing. Provide another way out in the footer or it is a dead end.

### `dismissLabel`

```ts
dismissLabel?: string = "Close"
```

Accessible name for the close button.

**Use when**

- Naming what closes when the word alone is ambiguous: "Close filters".

**Don't use for**

- Passing it with `dismissible={false}`. There is no button to name.

**Accessibility** Becomes the button's `aria-label`. The control is icon-only, so without a name it announces as "button".

### `initialFocus`

```ts
initialFocus?: RefObject<HTMLElement | null>
```

What to focus when it opens. Defaults to the platform's choice.

**Use when**

- The safe action in a destructive sheet, so the dangerous one is not one Enter away.

**Don't use for**

- Pointing it at something that is not rendered yet. It is applied once, after `showModal()`.

**Accessibility** Applied after `showModal()`, which is the only order that works — React's `autoFocus` runs during commit and the platform's own default overrides it.

### `className`

```ts
className?: string
```

Extra classes on the sheet, for placement — not for restyling it.

**Use when**

- Nothing yet. It exists for the same reason Dialog's does, and the same caution applies.

**Don't use for**

- Changing its geometry or colours. Those come from `edge`, `size` and the recipe.

## Use cases

### A bottom sheet of filters on a narrow viewport

The commonest use: a set of choices that would not fit beside the results, reachable where a thumb already is.

```tsx
const [filtering, setFiltering] = useState(false);

<Button onClick={() => setFiltering(true)}>Filters</Button>

<Sheet
  open={filtering}
  onClose={() => setFiltering(false)}
  title="Filters"
  description="Narrows the 1,204 results below."
  footer={
    <>
      <Button variant="secondary" onClick={() => setFiltering(false)}>
        Cancel
      </Button>
      <Button onClick={() => setFiltering(false)}>Apply</Button>
    </>
  }
>
  <SegmentedControl
    label="Period"
    value={period}
    onValueChange={setPeriod}
    items={[
      { value: "week", label: "Week" },
      { value: "month", label: "Month" },
    ]}
  />
</Sheet>
```

`edge` is left at its default, which is `block-end`. The footer holds the two ways out so neither is the close button, whose job is only to abandon the sheet.

### A detail drawer from the inline edge

The same content a Panel would hold, on a viewport too narrow to give the panel a column of its own.

```tsx
<Sheet
  open={selected !== null}
  onClose={() => setSelected(null)}
  edge="inline-end"
  size="lg"
  title={selected?.path ?? ""}
>
  <TokenDoc path={selected.path} />
</Sheet>
```

`inline-end` rather than `right`: the sheet follows the reading direction, so a right-to-left document gets it on the other side with no change here. This is the pattern's other half — the same content is a Panel when there is room for one, and the two take the same title.

### A required choice with no way to dismiss it

Something must be answered before the page behind it means anything — a workspace to act in, a term to accept.

```tsx
<Sheet
  open={needsWorkspace}
  onClose={() => {}}
  dismissible={false}
  size="sm"
  title="Choose a workspace"
  initialFocus={firstOptionRef}
>
  <RadioGroup
    label="Workspace"
    value={workspace}
    onValueChange={commitWorkspace}
    items={workspaces}
  />
</Sheet>
```

`dismissible={false}` removes the close button and blocks both Escape and the backdrop, so the content itself has to be the way out — here, picking an option commits it. A sheet with neither is a dead end, and a reader will reload the page to escape it.

## Real usage in this repo

```tsx
<Sheet
        open={path !== null}
        onClose={() => onClose()}
        edge="block-end"
        size="lg"
        title={<code>{shown ?? ""}</code>}
        dismissLabel={shown === null ? "Close" : `Close documentation for ${shown}`}
      >
        {shown !== null && <TokenDocBody path={shown} />}
      </Sheet>
```

## Token recipe

### base

A modal surface held against one edge. Unlike Dialog it is flush with that edge, so only the corners that face the page are rounded.

| Property | Token |
|---|---|
| background | `theme.bg.surface` |
| color | `theme.fg.primary` |
| border-radius on the two corners facing the page | `radius.page` |
| border-radius on the two corners against the edge | `radius.none — a radius there would show the page through the gap` |
| padding | `space.padding.lg` |
| box-shadow | `theme.elevation.modal` |
| gap between title, body, and footer | `space.stack.md` |
| gap between title and description | `space.stack.2xs` |
| title font-size | `type.heading.size` |
| title font-weight | `type.heading.weight` |
| title line-height | `type.heading.line-height` |
| description color | `theme.fg.secondary` |

### backdrop

The wash over the inert page behind it. The same treatment as Dialog's, because it is the same statement about the page.

| Property | Token |
|---|---|
| background | `theme.scrim` |
| fade | `motion.modal.duration with motion.easing.enter` |

### motion

It slides in from the edge it is anchored to. The distance is the sheet's own measure, so it starts fully off-screen whatever its size.

| Property | Token |
|---|---|
| enter transition | `motion.modal.duration with motion.easing.enter, translating from off-screen to rest` |
| exit transition | `motion.modal.duration with motion.easing.exit` |
| translate axis | `the axis the sheet's edge lies on — block for block-start and block-end, inline for inline-start and inline-end` |
| reduced motion | `the translate is dropped and only the backdrop's opacity transitions, at motion.modal.duration` |
| exit pointer-events | `none while closing — the top layer is held for the exit, so a click during it would land on a sheet that is leaving` |

### size

One measure across the sheet's own axis: block-size for a sheet on a horizontal edge, inline-size for one on a vertical edge. Derived from the control scale rather than stated in pixels, the same way Dialog's widths are.

| Property | Token |
|---|---|
| sm | `size.control.lg multiplied by 8` |
| md | `size.control.lg multiplied by 12` |
| lg | `size.control.lg multiplied by 18` |
| cap across its own axis | `100% of the dialog's containing block less space.padding.lg — stated as a percentage rather than in viewport units on purpose: vh and vw are the LARGE viewport, so a bottom sheet measured in vh is taller than the screen whenever the mobile address bar is showing. Dialog's insets are percentages for the same reason.` |
| measure across the other axis | `the full containing block — a sheet spans the edge it is anchored to. Not capped on wide viewports in this version; a bottom sheet that stops short of both sides is a different shape and would need its own entry here.` |

### footer

The actions, reading after the body they act on.

| Property | Token |
|---|---|
| gap between actions | `space.gap.sm` |
| margin above | `space.stack.md` |

### dismiss

The close control. Same construction as Dialog's, so a close button is the same control everywhere.

| Property | Token |
|---|---|
| color | `currentColor — inherited from the sheet's own foreground` |
| glyph size | `size.icon.text` |
| target inline-size / block-size | `size.control.sm` |
| border-radius | `radius.inner` |
| hover / press | `compose the .rata-state-layer class` |
| focus ring | `theme.focus-ring at focus.ring-width, offset focus.ring-offset` |
| block alignment | `centred on the title's first line, from type.heading.size and type.heading.line-height` |
